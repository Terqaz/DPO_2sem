{% extends 'base.html.twig' %}

{% block title %}Book{% endblock %}

{% block body %}
    <h1>{{ book.title }}</h1>
    <img src="/{{ book.coverUrl }}" alt="обложка" style="width: 300px">
    <table class="table">
        <tbody>
            <tr>
                <th>Автор</th>
                <td>{{ book.author }}</td>
            </tr>
            <tr>
                <td><a href="{{ book.fileUrl }}">Скачать</a></td>
            </tr>
            <tr>
                <th>Дата прочтения</th>
                <td>{{ book.dateRead ? book.dateRead|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="/{{ book.fileUrl }}" class="btn btn-outline-dark">Скачать</a>
    <a href="{{ path('app_book_edit', {'id': book.id}) }}">edit</a>
    <a href="{{ path('app_book_index') }}">back to list</a>


    {{ include('book/_delete_form.html.twig') }}
{% endblock %}
